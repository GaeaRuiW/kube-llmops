---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: llmplatforms.llmops.kubellmops.io
spec:
  group: llmops.kubellmops.io
  names:
    kind: LLMPlatform
    listKind: LLMPlatformList
    plural: llmplatforms
    shortNames:
    - lp
    - llmplatforms
    singular: llmplatform
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.components.gateway.phase
      name: Gateway
      type: string
    - jsonPath: .status.components.grafana.phase
      name: Grafana
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: LLMPlatform is the Schema for the llmplatforms API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: LLMPlatformSpec defines the desired state of the platform
              infrastructure.
            properties:
              gateway:
                description: Gateway configures the LiteLLM AI Gateway.
                properties:
                  budgetControl:
                    description: EnabledToggle is a simple enabled/disabled toggle.
                    properties:
                      enabled:
                        type: boolean
                    type: object
                  enabled:
                    type: boolean
                  image:
                    description: ImageSpec holds container image configuration.
                    properties:
                      repository:
                        type: string
                      tag:
                        type: string
                    type: object
                  langfuseEnabled:
                    description: LangfuseEnabled enables Langfuse callback integration
                      in LiteLLM.
                    type: boolean
                  langfusePublicKey:
                    description: LangfusePublicKey is the Langfuse public key for
                      LiteLLM callbacks.
                    type: string
                  langfuseSecretKey:
                    description: LangfuseSecretKey is the Langfuse secret key for
                      LiteLLM callbacks.
                    type: string
                  masterKey:
                    type: string
                  rateLimiting:
                    description: EnabledToggle is a simple enabled/disabled toggle.
                    properties:
                      enabled:
                        type: boolean
                    type: object
                  routing:
                    type: string
                type: object
              headlamp:
                description: Headlamp enables the Headlamp Kubernetes dashboard.
                properties:
                  enabled:
                    type: boolean
                type: object
              hfToken:
                description: HFToken is a HuggingFace API token for gated models.
                type: string
              ingress:
                description: Ingress configures ingress-based access.
                properties:
                  className:
                    type: string
                  enabled:
                    type: boolean
                  host:
                    type: string
                type: object
              keda:
                description: KEDA configures autoscaling.
                properties:
                  enabled:
                    type: boolean
                type: object
              keycloak:
                description: Keycloak configures SSO.
                properties:
                  enabled:
                    type: boolean
                type: object
              logging:
                description: Logging configures Fluent Bit + Loki.
                properties:
                  enabled:
                    type: boolean
                type: object
              modelStore:
                description: ModelStore configures the MinIO model cache.
                properties:
                  accessKey:
                    type: string
                  bucket:
                    type: string
                  enabled:
                    type: boolean
                  endpoint:
                    type: string
                  hfTransferConcurrency:
                    format: int32
                    type: integer
                  image:
                    type: string
                  secretKey:
                    type: string
                type: object
              models:
                description: Models defines the list of models to deploy.
                items:
                  description: ModelSpec defines a model to deploy.
                  properties:
                    engine:
                      description: Engine overrides auto-detection (vllm, tei, llamacpp,
                        sglang).
                      type: string
                    engineArgs:
                      additionalProperties:
                        type: string
                      description: EngineArgs are additional engine-specific arguments.
                      type: object
                    name:
                      description: Name is the short name used to reference this model.
                      type: string
                    replicas:
                      description: Replicas is the number of serving replicas.
                      type: integer
                    resources:
                      description: Resources specifies GPU, CPU, and memory requirements.
                      properties:
                        cpu:
                          default: "4"
                          description: CPU is the CPU limit (e.g. "4").
                          type: string
                        gpu:
                          default: 1
                          description: GPU is the number of GPUs to request (0 for
                            CPU-only).
                          format: int32
                          minimum: 0
                          type: integer
                        memory:
                          default: 16Gi
                          description: Memory is the memory limit (e.g. "16Gi").
                          type: string
                      type: object
                    source:
                      description: Source is the HuggingFace model ID or path.
                      type: string
                  required:
                  - name
                  - source
                  type: object
                type: array
              modules:
                description: Modules toggles high-level feature modules.
                properties:
                  finetune:
                    description: EnabledToggle is a simple enabled/disabled toggle.
                    properties:
                      enabled:
                        type: boolean
                    type: object
                  rag:
                    description: EnabledToggle is a simple enabled/disabled toggle.
                    properties:
                      enabled:
                        type: boolean
                    type: object
                  security:
                    description: EnabledToggle is a simple enabled/disabled toggle.
                    properties:
                      enabled:
                        type: boolean
                    type: object
                type: object
              nodePort:
                description: NodePort configures NodePort access to services.
                properties:
                  enabled:
                    type: boolean
                  host:
                    type: string
                type: object
              observability:
                description: Observability configures Prometheus + Grafana + Langfuse.
                properties:
                  enabled:
                    type: boolean
                  grafana:
                    description: GrafanaSpec configures Grafana.
                    properties:
                      adminPassword:
                        type: string
                      oidc:
                        description: EnabledToggle is a simple enabled/disabled toggle.
                        properties:
                          enabled:
                            type: boolean
                        type: object
                    type: object
                  langfuse:
                    description: EnabledToggle is a simple enabled/disabled toggle.
                    properties:
                      enabled:
                        type: boolean
                    type: object
                type: object
              postgresql:
                description: PostgreSQL configures the shared database.
                properties:
                  enabled:
                    type: boolean
                type: object
            type: object
          status:
            description: LLMPlatformStatus defines the observed state of LLMPlatform.
            properties:
              components:
                description: Components holds per-component health status.
                properties:
                  dify:
                    description: ComponentStatus holds the health of a single platform
                      component.
                    properties:
                      endpoint:
                        type: string
                      nodePort:
                        format: int32
                        type: integer
                      phase:
                        type: string
                    type: object
                  gateway:
                    description: ComponentStatus holds the health of a single platform
                      component.
                    properties:
                      endpoint:
                        type: string
                      nodePort:
                        format: int32
                        type: integer
                      phase:
                        type: string
                    type: object
                  grafana:
                    description: ComponentStatus holds the health of a single platform
                      component.
                    properties:
                      endpoint:
                        type: string
                      nodePort:
                        format: int32
                        type: integer
                      phase:
                        type: string
                    type: object
                  langfuse:
                    description: ComponentStatus holds the health of a single platform
                      component.
                    properties:
                      endpoint:
                        type: string
                      nodePort:
                        format: int32
                        type: integer
                      phase:
                        type: string
                    type: object
                  milvus:
                    description: ComponentStatus holds the health of a single platform
                      component.
                    properties:
                      endpoint:
                        type: string
                      nodePort:
                        format: int32
                        type: integer
                      phase:
                        type: string
                    type: object
                  minio:
                    description: ComponentStatus holds the health of a single platform
                      component.
                    properties:
                      endpoint:
                        type: string
                      nodePort:
                        format: int32
                        type: integer
                      phase:
                        type: string
                    type: object
                  postgresql:
                    description: ComponentStatus holds the health of a single platform
                      component.
                    properties:
                      endpoint:
                        type: string
                      nodePort:
                        format: int32
                        type: integer
                      phase:
                        type: string
                    type: object
                  prometheus:
                    description: ComponentStatus holds the health of a single platform
                      component.
                    properties:
                      endpoint:
                        type: string
                      nodePort:
                        format: int32
                        type: integer
                      phase:
                        type: string
                    type: object
                type: object
              conditions:
                description: Conditions represent the latest observations.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              helmRelease:
                description: HelmRelease is the name of the managed Helm release.
                type: string
              helmRevision:
                description: HelmRevision is the Helm release revision number.
                type: integer
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  by the controller.
                format: int64
                type: integer
              phase:
                description: Phase is the high-level lifecycle phase.
                enum:
                - Pending
                - Installing
                - Ready
                - Upgrading
                - Degraded
                - Failed
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}

apiVersion: llmops.kubellmops.io/v1alpha1
kind: LLMPlatform
metadata:
  labels:
    app.kubernetes.io/name: kube-llmops-operator
    app.kubernetes.io/managed-by: kustomize
  name: llmplatform-sample
spec:
  # TODO(user): Add fields here
